Set up the complete Cortier design environment. Add the public Cortier plugin marketplace from `https://github.com/cortier/codex-plugins` using its default branch, then install and enable the Cortier Design plugin from that marketplace. Install and enable the official Figma and Mobbin plugins from the Plugins Directory as independent plugins. Do not replace Figma or Mobbin with copied assets or standalone connections. Complete every required authentication flow: use a `cortier.com` Google Workspace identity for the Cortier Design MCP, and authenticate Figma and Mobbin with their intended accounts. Never ask me to paste credentials or tokens into the conversation; use the providers' authorization flows. After installation and authentication, perform one harmless read-only sanity check against each plugin's MCP: retrieve or inspect non-sensitive Figma account/file metadata without changing a file, perform a non-mutating Mobbin lookup, and fetch Cortier Design MCP protected-resource metadata or list its available tools without running a Figma operation. Do not make any write, edit, reflow, or destructive call during validation. Report installation complete only if all three plugins are installed and enabled, every required connection is authenticated, and all three read-only checks succeed. Otherwise, identify the incomplete plugin and continue its setup.
