Set up the complete Cortier design environment. Add the public Cortier plugin marketplace from `https://github.com/cortier/codex-plugins` using its default branch, then install and enable the Cortier Design plugin from that marketplace. Install and enable the official Figma and Mobbin plugins from the Plugins Directory as independent plugins. Initiate every required OAuth flow and wait for the user to complete each provider-controlled authorization interaction before continuing. Instruct the user to authenticate with media@cortier.com for all MCPs. After each OAuth flow returns control, verify the connection before proceeding. When all authentication is complete, list MCP tools of all plugins. Report installation complete only if all three plugins are installed and enabled, every required connection is authenticated, and all tools have been listed. Otherwise, identify the incomplete plugin and continue its setup.
